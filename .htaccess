# Keeps the app working when the project folder itself is the web root
# (e.g. http://localhost/lenscraft). Requests are forwarded into /public.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
